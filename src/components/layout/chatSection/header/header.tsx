import React from 'react';
import styles from "../styles.module.scss"
import {Stack} from "@mui/material";
const ChatHeader:React.FC = () => {
    return (
     <Stack className={styles.headerMain}>
         Header It Is)
     </Stack>
    );
};

export default ChatHeader;
